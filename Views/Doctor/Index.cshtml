@model IEnumerable<HospitalManagement.Models.MastDoctor>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="page-header space">
    <h2>Doctors</h2>
    <a href="@Url.Action("Create")" class="btn btn-primary">Add New</a>
</div>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Sl. No.</th>
            <th>Image</th>
            <th>Name</th>
            <th>Specialization</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var doctor in Model)
        {
            <tr>
                <td>@doctor.MastDoctorKey</td>
                <td><img src="@doctor.DoctorImage" width="50" height="50"></td>
                <td>@doctor.DoctorName</td>
                <td>@doctor.DoctorDegree</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = doctor.MastDoctorKey })" class="btn btn-warning">Edit</a>
                    @using (Html.BeginForm("Delete", "Doctor", new { id = doctor.MastDoctorKey }, FormMethod.Post, true, new { onsubmit = "return confirm('Are you sure you want to delete this doctor?');", style = "display:inline-block;" }))
                    {
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-danger">Delete</button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
